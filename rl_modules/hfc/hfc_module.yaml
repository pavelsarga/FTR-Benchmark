# HFCModule no longer has any module-specific parameters: get_reward_components() now
# mirrors MarvRLModule's reward exactly (driven entirely by the standard env_cfg_overrides
# reward coefficients — roll_coef, pitch_coef, shock_coef, etc., see marv_config_hfc.yaml),
# and get_observations() has no tunable knobs. Kept as an (empty) file only so
# HFCModule.__init__'s OmegaConf.load(_CONFIG_PATH) keeps working unchanged.
